let result = document.getElementById('result');

let x: number = 10;
let y: number = 30;

let xy: number = x + y;

console.log(xy);


const user: {name: string, password: number, bead: [string, number, string, boolean]} = {
    name: "heder",
    password: 12345,
    bead:["personagem", 1, "Ranger", true]
}

console.log(user);